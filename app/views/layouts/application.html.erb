<!DOCTYPE html>
<html>
  <head>
    <meta charset="uft-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title><%= full_title(yield(:title)) %></title>
    <meta name="description" content="設定した時間に入力されたテキストを音声でお知らせするサービスです。" >
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "sanitize.css" %>
    <%= stylesheet_link_tag "application", 'data-turbolinks-track': 'reload' %>

    <%# favicon %>
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <link rel="mask-icon" href="/favicon/safari-pinned-tab.svg" color="#5bbad5">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="theme-color" content="#ffffff">
  </head>

  <body id="<%= controller.controller_name %>-<%= controller.action_name %>">
    <header class="header">
        <div class="header-logo">
            <%= link_to image_tag("logo.png") ,root_path %>
        </div>
        <nav>
            <ul class="header-navlist">
                <li><%= link_to "ABOUT", about_path, class: "header-navlink" %></li>
                <li><%= link_to "CONTACT", new_contacts_path, class: "header-navlink" %></li>
            </ul>
        </nav>
    </header>
    <p><%= alert %></p>
    <%= yield %>
    <%= javascript_importmap_tags "application" %>
  </body>
</html>
